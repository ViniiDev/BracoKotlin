<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:gravity="center_horizontal"
    android:background="@drawable/fundo_tela">

    <!-- Espaço superior -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="55dp" />

    <!-- Título Principal -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ENVIAR COMANDOS"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/white"
        android:paddingBottom="24dp"
        android:layout_gravity="center_horizontal" />

    <!-- Animação do Microfone -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/chamaFuncaoDeEscutarMic"
        android:layout_width="237dp"
        android:layout_height="228dp"
        android:clickable="false"
        app:lottie_autoPlay="true"
        app:lottie_loop="true"
        app:lottie_rawRes="@raw/microfone_animation" />

    <!-- Espaço intermediário -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="51dp" />

    <!-- TextView para exibir o texto falado -->
    <TextView
        android:id="@+id/tvText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textSize="18sp"
        android:textColor="@color/white"
        android:gravity="center_horizontal"
        android:layout_gravity="center_horizontal"
        android:padding="16dp"/>
    <!--android:background="@drawable/text_background" />  Você pode criar um fundo customizado para o TextView, se necessário -->

</LinearLayout>
